import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gov.ga.dds.gaddsstaging',
  appName: 'DDS2GO',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
